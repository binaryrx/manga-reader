import styled from "styled-components";

export const LatestUpdates = styled.div`

`;

export const LatestUpdatesContent = styled.ul`
    display: flex;
    flex-flow: row wrap;
    padding:0;
    background-color: var(--manga-bg);
    box-shadow: var(--latestMangas-box-shadow);
`;
